#include <bits/stdc++.h>

using namespace std;

int main(void)
{
    string s;
    cin >> s;
    if(s.find("ss") != string::npos)
        cout << "hiss";
    else
        cout << "no hiss";
}