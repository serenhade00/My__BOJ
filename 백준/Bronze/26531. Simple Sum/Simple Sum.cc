#include <iostream>

using namespace std;

int main(void)
{
    int a, b, c;
    char d, e;
    
    cin >> a >> d >> b >> e >> c;
    
    if(a+b == c)
        cout << "YES";
    else
        cout << "NO";
}